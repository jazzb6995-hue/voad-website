/* ============================================================
   VOAD — Site Configuration
   ============================================================ */

const VOAD_CONFIG = {
  /* PHP API endpoint — works automatically when hosted on GoDaddy */
  apiUrl: '/api/projects.php',

  /* Contact form endpoint */
  contactUrl: '/api/contact.php',

  /* Set to false to always use local data (for offline development) */
  liveFromServer: true
};
